## inserted s___a.mut into ifeffit's memory...
set s___a_ratio = sin(pi*{$in}/180) / sin(pi*{$out}/180)
set(s___a.alpha = s___a.mut + s___a_ratio*{$muf},
    s___a.info = 10000*sin(pi*{$in}/180) / s___a.alpha)
