{
  $tic = int(log($xmax-$xmin)/log(10));
  $yrange = ($showy) ? "[0:]" : "[*:*]";
  q{}
}
set title "\{/*1.6 Fitting parameter\}"
set xrange [{$xmin}:{$xmax}]
set yrange {$yrange}
set xlabel "\{/*1.4 fit\}"
set xtics {10**$tic}
set ylabel "\{/*1.4 {$param}\}"
plot '{$file}' using 1:2:3 with errorbars ls {$P->increm+1} title '{$param}'
