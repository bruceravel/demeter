{ # -*- ifm -*-
  # GDS template
  #   {$D} returns the ifeffit group name
  #   {$D->get("parameter")} returns the value of that parameter
}
{
  $gds = $D->get('type');
  ($gds = 'set') if ($gds =~ m{(?:after|restrain|def)});
  ($gds = "# ".$gds) if ($gds =~ m{(?:merge|skip)});
  $gds = sprintf("%-7s", $gds);
  q{}
}
{$gds} {$D->get("name")} = {$D->get("mathexp")}

