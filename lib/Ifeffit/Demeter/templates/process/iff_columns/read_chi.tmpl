{ # -*- ifm -*-
  # import chi template
  #   {$D} returns the ifeffit group name
  #   {$D->get("parameter")} returns the value of that parameter
}
##| Reading chi data "{$D->get('file')}" as group {$D}
read_data(file  = "{$D->get('file')}",
          group = {$D},
	  type  = chi)
