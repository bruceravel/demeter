[% META title = 'The point finder' %]

[% FILTER html_para %]
The point finder is a tool for knowing the corresponding coordinate
for a point on a plot.  Use the
[% INCLUDE html/link text="the pluck buttons" link="ui/pluck.html" %]
to select a the x- or y-value of a point on the plot.  Clicking the
[% INCLUDE font/quote a="find Y" %] or [% INCLUDE font/quote a="find X" %]
button locates the corresponding coordinate on the most recent plot
and replots the data with a marker showing that point.

One good use of this tool is to compare the corresponding coordinate
for a given value between data groups.  Clicking on another group in
the group list and making a plot will not change the value found by
the pluck button.  Clicking the [% INCLUDE font/quote a="find" %] button
will find the corresponding coordinate for the new data group.
[% END %]


[% WRAPPER quadplot files=["plot_pf.png", "plot_pfplot.png"] %]
(Left) The point finder tab.
(Right) An example of a plot with the point finder.
[% END %]

[% FILTER html_para %]
The following preferences can be set to customize the appearance of
the point finder.
[% END %]

[% WRAPPER html/list type="ol" %]
[% WRAPPER html/li %]
[%- PROCESS preference group="Plot" option="pointfinder" %]: the
shape of the point finder marker
[% END %]
[% WRAPPER html/li %]
[%- PROCESS preference group="Plot" option="pointfindersize" %]: the
size of the point finder marker
[% END %]
[% WRAPPER html/li %]
[%- PROCESS preference group="Plot" option="pointfindercolor" %]: the
color of the point finder marker
[% END %]
[% END %]
